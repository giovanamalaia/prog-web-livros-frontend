{
  "name": "livros-frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "tsc -p typescript/tsconfig.json",
    "dev": "npm run build && python3 -m http.server 5500 --bind 127.0.0.1 --directory public"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
