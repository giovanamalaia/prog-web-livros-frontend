{
  "name": "livros-frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && python3 -m http.server 5500 --bind 127.0.0.1"
  },
  "devDependencies": {
    "typescript": "~6.0.2"
  }
}
